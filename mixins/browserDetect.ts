// import browserDetect from 'detect-browser'

// export default function(params: string = 'name,version,os') {
//   const browser = browserDetect.detect()

//   if (!browser) {
//     return
//   }

//   params.split(',').forEach(param => {
//     document.querySelector('html').classList.add(browser[param].split('.')[0].replace(/\s+/g, ''))
//   })
// }
