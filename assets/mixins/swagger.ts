// Swagger usage example
// import { PlacesApi } from '../libs/swagger'

// (async () => {
//   const api = new PlacesApi({ basePath: ' ' })
//   const data = await api.getPlaces()

//   console.log(data)

//   data.forEach(item => {
//     console.log(item.id, item.name)
//   })
// })()
